{
  "name": "grpc-client",
  "version": "0.0.1",
  "description": "An Electron based client for gRPC",
  "main": "app/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "electron": "^1.7.5"
  }
}
